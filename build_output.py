# Real progress build_output is installed via:
#   python scripts/install_real_progress.py
# or:
#   python scripts/assemble_build_output.py
#
# This stub exists so git pull does not leave a broken PLACEHOLDER.
# Run one of the installers above, then: dboc build --variant taiwan
raise SystemExit(
    "build_output.py not installed yet.\n"
    "Run:  python scripts/install_real_progress.py\n"
    "Then: dboc build --variant taiwan"
)
