# data/deltas/

| 檔案 | 內容 |
|------|------|
| `term.tsv` | 用字／地名（凱里、防具、高級…） |
| `ui.tsv` | UI／lang0 |
| `tbl.tsv` | 全部 tbl 批次翻譯（原 tbl_batch* 已合併） |

由 `scripts/consolidate_deltas.py` 產生。`merge_translations.py` **只讀這裡**。
