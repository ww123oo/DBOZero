# data/archive/

歷史備份與已合併的 delta。**日常翻譯只改：**

- `data/new_translations.tsv`
- `data/translations.tsv`

根目錄 `data/*_delta.tsv`、`data/tbl_batch*_delta.tsv` 多為一次修補紀錄；
若已併入主表，勿再當現行來源重套。之後可逐步移入本目錄以保持 `data/` 乾淨。
